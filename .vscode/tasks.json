{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "shell",
			"label": "build",
			"command": "sh ${workspaceFolder}/scripts/b"
		}, 
		{ 
			"label": "run lit tests",
			"type": "shell",
			"command": "ninja -C ${workspaceFolder}/build/ lit"
		}
	]
}